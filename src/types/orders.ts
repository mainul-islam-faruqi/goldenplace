export interface TakerOrder {
  // signer(signer: any, arg1: { cacheTime: number; });
}

export interface MakerOrderWithSignatureAndHash {
  // signer(signer: any, arg1: { cacheTime: number; });
}

export interface MakerOrderWithSignature {
  // signer(signer: any, arg1: { cacheTime: number; });
}

export interface MakerOrderWithVRS {}
export interface MakerOrder {}
export interface SolidityType {}
